# Aidit-Code — 프로젝트 작업 규칙 (AGENTS.md)

> 이 파일은 [`CLAUDE.md`](./CLAUDE.md)와 동일한 규칙을 교차 에이전트(Codex·Cursor·OMC 등)용으로 둔다. 두 파일은 같은 내용을 유지한다.

## Ground Rules (필수)

### GR-1. 코드 수정 전 문서 먼저 업데이트 (Docs-before-code)
모든 **코드 추가/수정** 작업은 **코드를 건드리기 전에 먼저 문서를 업데이트**한다.

- 적용 대상: 기능 추가(feat)·버그 수정(fix)·동작 변경·리팩터링 등 **소스 코드가 바뀌는 모든 명령**.
- 순서는 항상 다음 5단계를 따른다:
  1. **① 문서 업데이트** — 변경 이력에 **착수 예정 항목**을 먼저 적는다(상태: `진행중`).
  2. **② 구현** — 코드를 작성/수정한다.
  3. **③ 검증** — typecheck/test/lint 등으로 동작을 확인한다.
  4. **④ 문서에 완료 표시** — 검증을 통과한 항목만 상태를 `완료`로 바꾸고, 실제 변경 파일·결과를 정정 반영한다.
  5. **⑤ 커밋·푸시** — 위가 모두 끝난 뒤에만 커밋·푸시한다.
- **검증을 통과하기 전에는 절대 `완료`로 표시하지 않는다.** 검증 실패 시 항목은 `진행중`으로 두고 ②~③을 반복한다.
- 기본 문서는 [`docs/IMPLEMENTATION_NOTES.md`](./docs/IMPLEMENTATION_NOTES.md)의 변경 이력(Changelog)이다.
  최신 항목을 맨 위에 두고, 태그(`[feat]`/`[fix]`/`[test]`/`[docs]`/`[chore]`)와
  변경 파일 경로, 상태(`진행중`/`완료`)를 함께 적는다. 날짜는 절대 날짜로 기록한다.
- 스펙·계약·동작이 바뀌면 관련 상위 문서(`PRD.md`/`TRD.md`/`PLAN.md`/`WIREFRAME.md`)의
  해당 서술도 함께 갱신하고, 옛 서술이 새 동작과 모순되면 정정한다.
- 사소한 오타/포매팅 등 동작이 전혀 바뀌지 않는 변경은 이 규칙에서 제외한다.

## 참고
- 문서 인덱스/구조: `docs/` (PRD·TRD·PLAN·WIREFRAME·IMPLEMENTATION_NOTES).
- 그린 인광 CRT 레트로 터미널 디자인 시스템은 부모 Aidit에서 그대로 계승한다(PRD/WIREFRAME 참조).
- 사용자 노출 문자열은 하드코딩하지 말고 i18n(`t('namespace.key')`)을 사용한다.
- LLM 키(`API_KEY`/`BASE_URL`/`MODEL`)는 **서버 `.env`** 에만 두고, 클라이언트·로그·응답에 절대 노출하지 않는다(TRD §2·§8).
